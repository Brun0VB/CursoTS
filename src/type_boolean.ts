let confirma: boolean;// false ou true

//confirma = true;
let code: number = 0;

confirma = Boolean(code); // B maiusculo => método boolean que converte o argumento em booleano

console.log(confirma);